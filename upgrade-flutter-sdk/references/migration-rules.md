# Flutter Android/iOS migration rules

Use this catalog after the inspector selects the levels crossed by the requested upgrade. Conditions matter: not every item requires a file change.

## 3.29

### Android

- Search app and local plugin sources for `PluginRegistry.Registrar`, `FlutterMain`, old `io.flutter.app.*` imports, and legacy `registerWith`. Migrate matches to Android embedding v2. No change is needed when the project already uses `FlutterActivity`/`FlutterFragmentActivity` and `FlutterPlugin`.
- Compare the Android toolchain with a target-version disposable template. The observed `tcm-app` upgrade changed Gradle `8.3 → 8.10.2`; the clean Flutter 3.29.3 template uses Gradle 8.10.2, AGP 8.7.0, Kotlin 1.8.22, and Java/JVM target 11. Keep newer compatible project values.
- Add `app/.cxx/` to `android/.gitignore` when native builds can generate it and it is not already ignored.

### iOS

- The historical project added `enableGPUValidationMode = "1"` to the launch action. Treat this as template alignment, not a build requirement.
- Keep the iOS 12 deployment baseline unless dependencies require higher; Flutter raises the default at 3.35.

### Root

- Flutter 3.29.3 includes Dart 3.7.2. Ensure the Dart range accepts the exact target patch's bundled Dart SDK.
- Synchronize existing project pins and CI clone branches. Do not infer that dependency packages need bulk updates.

## 3.32

### Android

- Search scripts and Gradle configuration for `.flutter-plugins`. Replace parsing with `.flutter-plugins-dependencies` or, preferably, the declarative `dev.flutter.flutter-plugin-loader` approach.
- Ensure the Flutter Gradle plugins use the declarative `plugins {}` DSL. Projects already generated by Flutter 3.16+ usually need no action.

### Root

- Keep `.flutter-plugins` and `.flutter-plugins-dependencies` ignored/generated. Run `flutter pub get` to regenerate metadata.
- Check custom localization tooling because generated localization output no longer relies on a synthetic package.

## 3.35

### Android

- Search all Gradle variants for `abiFilters`.
  - No custom filters: no edit is required.
  - Single custom APK: clear Flutter's defaults and set the intended filters in `defaultConfig`.
  - Filters in `buildTypes` or `productFlavors`: validate using `-Pdisable-abi-filtering=true` or restructure them.
  - Prefer `flutter build apk --split-per-abi` when separate APKs are acceptable.

### iOS

- Set the minimum supported iOS version to `13.0` in all applicable places:
  - `ios/Flutter/AppFrameworkInfo.plist` → `MinimumOSVersion`.
  - Every project/target configuration in `ios/Runner.xcodeproj/project.pbxproj` → `IPHONEOS_DEPLOYMENT_TARGET`.
  - `ios/Podfile` → `platform :ios, '13.0'` when the file declares or documents a platform. Prefer an active declaration when pods need it.
- Add `customLLDBInitFile = "$(SRCROOT)/Flutter/ephemeral/flutter_lldbinit"` to both `TestAction` and `LaunchAction` in the shared Runner scheme if absent. Preserve every other scheme attribute.
- Run `pod install` after deployment-target or Podfile changes.

### Root

- Ensure the Dart SDK constraint accepts the Dart version bundled by the exact target patch. Do not copy `tcm-app`'s dependency upgrades; they were application maintenance, not Flutter host migration requirements.

## 3.38

### Android

- Require a compatible Java runtime. Flutter 3.38 has a minimum supported Java version of 17; set Java source/target and Kotlin JVM target to 17 unless the project deliberately uses a newer compatible level.
- Align the toolchain as a compatible set. Flutter 3.38.10's clean-template baseline is:
  - Gradle wrapper: `8.14`
  - Android Gradle Plugin: `8.11.1`
  - Kotlin Gradle Plugin: `2.2.20`
- Check Firebase, Google Services, Crashlytics, KSP, code-quality, and other Gradle plugins before raising AGP/Kotlin. Keep later compatible versions.
- Kotlin DSL is the modern template format, but Groovy remains valid. Do not rename `build.gradle`/`settings.gradle` to `.kts` as a routine upgrade. If explicitly migrating, translate every custom signing, flavor, repository, ProGuard, and task block semantically and validate Gradle before deleting Groovy files.
- Remove `android.enableJetifier=true` only if dependency inspection/build confirms Jetifier is unnecessary.
- JVM heap increases seen in `tcm-app` are project tuning, not a Flutter requirement.

### iOS

- Treat UIScene adoption as a semantic migration:
  - Inspect `AppDelegate` for plugin registration, method channels, platform views, window/root controller access, deep links, notifications, and UI-state callbacks.
  - Add the Application Scene Manifest to `Info.plist` when adopting UIScene.
  - Make `AppDelegate` conform to `FlutterImplicitEngineDelegate` and move plugin/channel setup to `didInitializeImplicitFlutterEngine` as required.
  - Use `FlutterSceneDelegate` or a project-specific subclass for scene lifecycle callbacks.
- Flutter 3.38 introduces the APIs; automatic eligible-app migration becomes default in 3.41. For a 3.38 target, explain whether migration was applied, deferred, or blocked by custom AppDelegate logic.

### Root

- Ensure the Dart constraint accepts the target patch's bundled Dart SDK (3.38.10 bundles Dart 3.10.9).
- Let Flutter update `.metadata` revision hashes. Do not paste the example project's revision unless it exactly matches the installed SDK.

## Uncataloged target levels

For any crossed `major.minor` not listed here:

1. Read Flutter's official breaking-change index and release notes for every missing stable level.
2. Generate fresh Android/iOS-only apps with the source and target SDKs in temporary directories using the same project language choices when possible.
3. Diff root metadata plus `android/` and `ios/`, excluding generated outputs and identifiers.
4. Classify each delta as required compatibility, conditional migration, template alignment, or unrelated formatting.
5. Merge only relevant deltas into the existing project and record the newly discovered rule instead of guessing.
